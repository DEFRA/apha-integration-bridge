/**
 * The Salesforce operation (for example, `addKeyFacts`) that was running when
 * the error occurred, so callers know which step failed.
 * @typedef {Error & {operation?: string}} SalesforceOperationError
 */
