/**
 * @typedef {'activation' | 'updated'} WorkorderDateFilterType
 */

export const WorkorderDateFilterType = {
  ACTIVATION: /** @type {const} */ ('activation'),
  UPDATED: /** @type {const} */ ('updated')
}
