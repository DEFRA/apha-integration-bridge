# Create Case

Creates a new case in Salesforce using a composite API request.

## Request

The endpoint accepts a composite request payload containing multiple sub-requests that are executed transactionally.

## Response

Returns the created case details with a 200 status code on success.
